---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: localhost-ingress
spec:
  rules:
  - http:
      paths:
      - path: /*
        pathType: Prefix
        backend:
          service:
            name: my-service
            port:
              name: http